interface:
  display_name: "GTK-OSX Release Updater"
  short_description: "Update direct release pins with verified hashes"
  default_prompt: "Use $gtk-osx-release-updater to find or apply direct upstream project releases one commit at a time."
